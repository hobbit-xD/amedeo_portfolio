import styled from 'styled-components';

export const SocialMediaStyled = styled.div`
    display: flex;
    flex-direction: row;

    @media (max-width: 768px) {
        flex-direction: row;
        margin: 0px;
    }
`;
